<resources>
    <!-- Minimal Android theme — Compose handles its own theming via MaterialTheme. -->
    <style name="Theme.Spacex.NoActionBar" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/blue_700</item>
    </style>
</resources>